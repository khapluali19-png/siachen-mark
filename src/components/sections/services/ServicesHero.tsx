import PageHeader from "@/components/layout/PageHeader";

export default function ServicesHero() {
  return (
    <PageHeader
      title="Everything Your Brand Needs to Grow — In One Place."
      subtitle="From strategy to design to tracking, we build the full system — not just the pieces."
    />
  );
}
